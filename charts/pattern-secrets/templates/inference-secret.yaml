apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: inference
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStoreRef.name }}
    kind: {{ .Values.secretStoreRef.kind }}
  target:
    name: inference
    template:
      type: Opaque
      data:
        provider: {{ printf "{{ .provider }}" | quote }}
        model: {{ printf "{{ .model }}" | quote }}
        api_key: {{ printf "{{ .api_key }}" | quote }}
        base_url: {{ printf "{{ .base_url }}" | quote }}
  data:
    - secretKey: provider
      remoteRef:
        key: {{ .Values.vaultPrefix }}/inference
        property: provider
    - secretKey: model
      remoteRef:
        key: {{ .Values.vaultPrefix }}/inference
        property: model
    - secretKey: api_key
      remoteRef:
        key: {{ .Values.vaultPrefix }}/inference
        property: api_key
    - secretKey: base_url
      remoteRef:
        key: {{ .Values.vaultPrefix }}/inference
        property: base_url
